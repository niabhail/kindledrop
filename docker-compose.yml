services:
  kindledrop:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - TZ=${TZ:-UTC}
    restart: unless-stopped
